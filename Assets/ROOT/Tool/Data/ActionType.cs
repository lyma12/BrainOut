public enum ActionType
{
    PlayAnimation,
    SetScale,
    SetPosition,
    SetActive,
    PlaySound,
    Wait
}

public enum ActionTarget
{
    Self,
    Other
}

public enum TransitionDirection
{
    None,
    Left,
    Right,
    Up,
    Down
}
